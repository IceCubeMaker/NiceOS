{ lib, config, pkgs, ... }:

{
  # ── Screenscraper ─────────────────────────────────────────────────────────
  # Register at https://www.screenscraper.fr
  # Format: "username:password"
  global.screenscraperUser = "username:password";
}